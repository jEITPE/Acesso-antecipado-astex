[phases.setup]
aptPkgs = ['nodejs']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run preview'

[variables]
NODE_VERSION = "18.x"
NPM_CONFIG_LEGACY_PEER_DEPS = "true"
